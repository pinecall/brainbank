# BrainBank Examples

| Example | Description | Run |
|---------|-------------|-----|
| [chatbot](chatbot/) | CLI chatbot with streaming + persistent memory (context injection + function calling) | `OPENAI_API_KEY=sk-... npx tsx examples/chatbot/chatbot.ts` |
| [collections](collections/) | Collections, semantic search, tags, metadata linking, TTL | `npx tsx examples/collections/collections.ts` |

Each example has its own README with architecture details and usage instructions.

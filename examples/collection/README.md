# Collection Example

Store and search structured agent memory using **KV collections** with hybrid search.

## Run

```bash
npx tsx examples/collection/collection.ts
```

No API keys needed — uses local WASM embeddings.

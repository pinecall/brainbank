/**
 * BrainBank — Embedding Provider Interface
 * 
 * Re-exports the EmbeddingProvider interface from types.
 * Provides the base contract for pluggable embedding backends.
 */

export type { EmbeddingProvider } from '../types.ts';
